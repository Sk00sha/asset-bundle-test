bundle:
  name: demo-job-bundle

resources:
  jobs:
    demo_job:
      name: demo-asset-bundle-job
      tasks:
        - task_key: run_script
          spark_python_task:
            python_file: ../src/job.py
          compute:
            type: serverless
